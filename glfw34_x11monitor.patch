diff --git a/src/x11_monitor.c b/src/x11_monitor.c
index 38af7e0..c9bbdb9 100644
--- a/src/x11_monitor.c
+++ b/src/x11_monitor.c
@@ -150,6 +150,13 @@ void _glfwPollMonitorsX11(void)
             }

             XRRCrtcInfo* ci = XRRGetCrtcInfo(_glfw.x11.display, sr, oi->crtc);
+            if (!ci)
+            {
+                // CRTC not yet ready during hotplug; skip this output
+                XRRFreeOutputInfo(oi);
+                continue;
+            }
+
             if (ci->rotation == RR_Rotate_90 || ci->rotation == RR_Rotate_270)
             {
                 widthMM  = oi->mm_height;
@@ -239,6 +246,11 @@ void _glfwSetVideoModeX11(_GLFWmonitor* monitor, const GLFWvidmode* desired)
         XRRScreenResources* sr =
             XRRGetScreenResourcesCurrent(_glfw.x11.display, _glfw.x11.root);
         XRRCrtcInfo* ci = XRRGetCrtcInfo(_glfw.x11.display, sr, monitor->x11.crtc);
+        if (!ci)
+        {
+            XRRFreeScreenResources(sr);
+            return;
+        }
         XRROutputInfo* oi = XRRGetOutputInfo(_glfw.x11.display, sr, monitor->x11.output);

         for (int i = 0;  i < oi->nmode;  i++)
@@ -288,6 +300,11 @@ void _glfwRestoreVideoModeX11(_GLFWmonitor* monitor)
         XRRScreenResources* sr =
             XRRGetScreenResourcesCurrent(_glfw.x11.display, _glfw.x11.root);
         XRRCrtcInfo* ci = XRRGetCrtcInfo(_glfw.x11.display, sr, monitor->x11.crtc);
+        if (!ci)
+        {
+            XRRFreeScreenResources(sr);
+            return;
+        }

         XRRSetCrtcConfig(_glfw.x11.display,
                          sr, monitor->x11.crtc,
@@ -357,23 +374,27 @@ void _glfwGetMonitorWorkareaX11(_GLFWmonitor* monitor,
             XRRGetScreenResourcesCurrent(_glfw.x11.display, _glfw.x11.root);
         XRRCrtcInfo* ci = XRRGetCrtcInfo(_glfw.x11.display, sr, monitor->x11.crtc);

-        areaX = ci->x;
-        areaY = ci->y;
+        if (ci)
+        {
+            areaX = ci->x;
+            areaY = ci->y;

-        const XRRModeInfo* mi = getModeInfo(sr, ci->mode);
+            const XRRModeInfo* mi = getModeInfo(sr, ci->mode);

-        if (ci->rotation == RR_Rotate_90 || ci->rotation == RR_Rotate_270)
-        {
-            areaWidth  = mi->height;
-            areaHeight = mi->width;
-        }
-        else
-        {
-            areaWidth  = mi->width;
-            areaHeight = mi->height;
+            if (ci->rotation == RR_Rotate_90 || ci->rotation == RR_Rotate_270)
+            {
+                areaWidth  = mi->height;
+                areaHeight = mi->width;
+            }
+            else
+            {
+                areaWidth  = mi->width;
+                areaHeight = mi->height;
+            }
+
+            XRRFreeCrtcInfo(ci);
         }

-        XRRFreeCrtcInfo(ci);
         XRRFreeScreenResources(sr);
     }
     else
@@ -452,6 +473,17 @@ GLFWvidmode* _glfwGetVideoModesX11(_GLFWmonitor* monitor, int* count)
         XRRCrtcInfo* ci = XRRGetCrtcInfo(_glfw.x11.display, sr, monitor->x11.crtc);
         XRROutputInfo* oi = XRRGetOutputInfo(_glfw.x11.display, sr, monitor->x11.output);

+        if (!ci || !oi)
+        {
+            if (ci)  XRRFreeCrtcInfo(ci);
+            if (oi)  XRRFreeOutputInfo(oi);
+            XRRFreeScreenResources(sr);
+            result = _glfw_calloc(1, sizeof(GLFWvidmode));
+            _glfwGetVideoModeX11(monitor, result);
+            *count = 1;
+            return result;
+        }
+
         result = _glfw_calloc(oi->nmode, sizeof(GLFWvidmode));

         for (int i = 0;  i < oi->nmode;  i++)
